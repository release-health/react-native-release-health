---
"react-native-release-health": minor
---

Add the health engine and public API: `ReleaseHealth.init()` with pluggable OTA adapters and sinks, `markHealthy()` probation tracking with crash-loop detection and rollback recommendations, `onRollbackRecommended()`, and the `useReleaseHealth()` hook.
