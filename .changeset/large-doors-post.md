---
"@release-health/sink-http": minor
---

Initial release: batching HTTP sink that posts release-health events as JSON to any endpoint, with retry buffering and configurable batch size and flush interval.
