---
"@release-health/adapter-expo-updates": minor
---

Initial release: expo-updates adapter mapping the expo-updates state machine to the OtaAdapter contract. Edge-triggered downloaded/error events, embedded-launch detection, and graceful degradation when expo-updates is missing or disabled. Recommendation-only: expo-updates has no client-side rollback API.
